name: Build MCP AIOps Server

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/reusable-build-python-container-image.yml"
      - ".github/workflows/build-tools-mcp-aiops-server.yml"
      - "tools/mcp/aiops_server/**"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/reusable-build-python-container-image.yml"
      - ".github/workflows/build-tools-mcp-aiops-server.yml"
      - "tools/mcp/aiops_server/**"
  release:
    types: [published]
  workflow_run:
    workflows: ["Build Python Base Image"]
    types:
      - completed
    branches:
      - main

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build-python-container-image.yml
    with:
      image-name: tools-mcp-aiops-server
      context-path: ./tools/mcp/aiops_server
